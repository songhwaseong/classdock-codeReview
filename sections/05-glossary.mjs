// 용어 사전 — 이 리뷰에 나오는 말 중 처음 보면 막히는 것들.
//
// 고를 때 짐작하지 않았다. 리뷰 산문의 어휘 빈도를 세어, 실제로 자주 나오면서 초급자가 모르면
// 문장 전체를 놓치는 말만 담았다(전역 850회, 계층 169회, 폴백 68회 …).
// 각 항목의 match 는 생성 스크립트가 "사전에는 있는데 리뷰 본문에는 없는 말"을 찾는 데 쓴다.
// 리뷰 문장이 바뀌어 안 쓰게 된 말은 빌드 경고로 드러나므로 사전이 조용히 낡지 않는다.
//
// ── 반대 방향은 왜 자동으로 못 잡는가 ────────────────────────────────
// "리뷰에 새로 자주 나오게 됐는데 사전에 없는 말"은 자동으로 찾지 못한다. 빈도만 세면 파일(645)
// ·실행(295)·저장(233) 같은 평범한 한국어가 상위를 채우고, 정작 넣어야 할 타일(20)·지오코딩(2)
// 은 한참 아래에 묻힌다. "자주 나오는 말"과 "설명이 필요한 말"이 다르기 때문이며, 이 판단은
// 사람이 해야 한다(실제로 n-gram·제목 후보 두 방식을 재 봤지만 둘 다 노이즈가 신호를 덮었다).
//
// 그래서 낱말을 맞히려 들지 않고, "다시 훑어볼 때가 됐다"는 신호만 정확하게 남긴다. 아래
// CURATED 는 이 사전을 마지막으로 손으로 훑었을 때의 리뷰 규모와 주제 묶음이다. 산문이 눈에
// 띄게 늘거나 새 주제 묶음이 생기면 생성 스크립트가 알린다 — 지도가 통째로 들어왔을 때
// 아무 신호도 없었던 것이 이 장치를 둔 이유다.
export const CURATED = {
  // 마지막으로 어휘 빈도를 다시 센 시점의 리뷰 산문 길이(글자). 2026-09-13 갱신.
  proseChars: 260146,
  // 그때 리뷰에 있던 주제 묶음(섹션 group). 여기 없는 묶음이 생기면 새 주제가 들어온 것이다.
  groups: [
    '전체 구조', '용어 사전', '코드 낱말 사전', '코드 기호·기본 기능 사전',
    '코드 메서드·실행 패턴 사전', '프로젝트 API 사전',
    '계층 개요', 'bootstrap', 'documents', 'python-and-notebooks', 'javascript',
    'document-editors', '지도', '연대표', '관계도 · 암기', 'learning-tools', '악보', '원격 터미널',
    'DB 클라이언트', 'java', 'snippet-gallery',
    '개요', '기동', '보안', '파일', '기능별', 'Python', 'Java', '변환 · DB', '진단', '시험지',
    '빌드', '검사', '자산', '테스트 인프라', '기타 도구', '단위', '계약', 'E2E', '공백',
  ],
  // 산문이 이만큼(비율) 넘게 늘면 다시 훑을 때가 됐다고 본다.
  growthTolerance: 0.1,
};

const TERMS = [
  // ── 뼈대: 이 프로젝트를 읽기 전에 알아야 하는 말 ────────────────────
  {
    theme: '뼈대',
    term: '전역 스크립트',
    en: 'global script',
    match: ['전역 스크립트', '전역'],
    body:
      '파일을 나눠 놓긴 했지만 서로 import 하지 않고, 브라우저가 순서대로 읽어 들이면서 만든 이름을 모두가 같은 공간에 쌓아 두는 방식입니다. ' +
      '즉 a.js 가 만든 함수를 b.js 가 아무 선언 없이 그냥 부릅니다. 대신 "누가 먼저 읽히는가"가 곧 규칙이 되고, 이름이 겹치면 나중 것이 앞의 것을 덮어씁니다. ' +
      '이 리뷰에서 가장 많이 나오는 말이며, 이 프로젝트의 거의 모든 제약이 여기서 나옵니다.',
  },
  {
    theme: '뼈대',
    term: '로딩 순서 · 로딩 계층',
    en: 'load order / layer',
    match: ['로딩 순서', '계층'],
    body:
      'HTML 에 적힌 <script> 태그의 위에서 아래 순서가 곧 실행 순서입니다. 이 프로젝트는 그 순서를 여섯 덩어리(bootstrap → documents → python → javascript → editors → learning-tools)로 묶고 "계층"이라 부릅니다. ' +
      '아래 계층은 위 계층이 만든 이름을 쓸 수 있지만 그 반대는 안 됩니다. 위 계층이 실행될 때 아래 것은 아직 존재하지 않기 때문입니다.',
  },
  {
    theme: '뼈대',
    term: 'ES module',
    en: 'import / export',
    match: ['ES module', 'import'],
    body:
      '요즘 표준 방식으로, 파일마다 "내보낼 것(export)"과 "가져올 것(import)"을 명시해 의존 관계가 코드에 그대로 드러납니다. ' +
      '이 앱은 이것을 쓰지 않습니다. 그래서 의존 관계를 코드에서 읽어 낼 수 없고, 대신 manifest 라는 별도 파일에 손으로 적어 관리합니다.',
  },
  {
    theme: '뼈대',
    term: '의존',
    en: 'dependency',
    match: ['의존'],
    body:
      'A 가 제대로 돌려면 B 가 먼저 있어야 할 때 "A 는 B 에 의존한다"고 합니다. ' +
      '모듈 시스템이 없는 이 프로젝트에서 의존은 곧 로딩 순서 문제라서, 순서가 뒤집히면 "정의되지 않음(undefined)" 오류로 터집니다.',
  },
  {
    theme: '뼈대',
    term: '공개 API · 경계',
    en: 'public API / module boundary',
    match: ['공개 API', 'publicApi', 'moduleBoundaries', '경계'],
    body:
      '"이 파일 안의 함수는 마음대로 고쳐도 되지만, 이 이름 하나만은 밖과의 약속이니 함부로 바꾸지 말자"고 정해 둔 창구입니다. ' +
      '이 앱에서는 MNLazy 처럼 MN 으로 시작하는 이름이 그 창구이고, 누가 그것을 쓰는지까지 manifest 에 적어 둡니다.',
  },
  {
    theme: '뼈대',
    term: 'manifest',
    en: 'scripts.manifest.json',
    match: ['manifest'],
    body:
      '"목록을 적어 둔 문서"라는 뜻입니다. 이 프로젝트의 scripts.manifest.json 은 파일 로딩 순서·계층·의존·공개 API 를 데이터로 들고 있습니다. ' +
      '문서에 글로 적어 두면 코드와 어긋나도 아무도 모르지만, 데이터로 두면 검사 도구가 코드와 대조해 어긋남을 빌드 실패로 잡을 수 있습니다.',
  },
  {
    theme: '뼈대',
    term: '계약',
    en: 'contract',
    match: ['계약'],
    body:
      '"이 이름, 이 인자, 이 응답 모양은 바꾸지 않는다"는 약속입니다. 코드가 강제하는 게 아니라 사람이 정하는 것이라, 지켜지는지 확인하려면 따로 검사(계약 테스트)가 필요합니다. ' +
      '이 리뷰의 "계약 · 인터페이스" 패널이 그 약속들을 모아 둔 곳입니다.',
  },
  {
    theme: '뼈대',
    term: '폴백',
    en: 'fallback',
    match: ['폴백'],
    body:
      '"제일 좋은 방법이 안 되면 차선책으로 물러선다"는 뜻입니다. 이 앱은 EXE(로컬 서버)가 있으면 진짜 디스크에 저장하고, 없으면 브라우저 다운로드로 물러섭니다. ' +
      '리뷰에 68번 나올 만큼 이 코드베이스 전체를 관통하는 패턴이며, 한쪽만 고치고 다른 쪽을 잊는 것이 이 구조의 대표적인 사고 지점입니다.',
  },
  {
    theme: '뼈대',
    term: '지연 로드',
    en: 'lazy load',
    match: ['지연 로드', 'MNLazy'],
    body:
      '무거운 파일을 시작할 때 미리 싣지 않고, 실제로 필요해진 순간에 불러오는 방식입니다. ' +
      'PDF 를 열지 않는 사용자에게 PDF 라이브러리를 처음부터 읽히면 시작만 느려지므로, 이 앱은 그런 무거운 것들을 열 때까지 미뤄 둡니다.',
  },
  {
    theme: '뼈대',
    term: 'vendor',
    en: '외부 라이브러리 고정본',
    match: ['vendor'],
    body:
      '남이 만든 라이브러리를 인터넷에서 그때그때 받아 쓰지 않고, 특정 버전 파일을 프로젝트 안에 복사해 둔 것입니다. ' +
      '오프라인에서도 똑같이 동작하고 버전이 몰래 바뀌지 않는 대신, 보안 수정이 나와도 자동으로 따라오지 않아 직접 갱신해야 합니다.',
  },
  {
    theme: '뼈대',
    term: '번들러 · 프레임워크',
    en: 'bundler / framework',
    match: ['번들러', '프레임워크'],
    body:
      '번들러(webpack·Vite 등)는 여러 파일을 하나로 합쳐 주는 도구이고, 프레임워크(React·Vue 등)는 화면 만드는 방식을 정해 주는 틀입니다. ' +
      '이 앱은 둘 다 쓰지 않습니다. 배우고 설치할 것이 없고 파일을 열면 바로 도는 대신, 그 도구들이 대신 해 주던 일(순서 관리·이름 충돌 방지)을 사람이 규칙으로 지켜야 합니다.',
  },

  // ── 웹·브라우저 ───────────────────────────────────────────────────
  {
    theme: '웹·브라우저',
    term: 'DOM',
    en: 'Document Object Model',
    match: ['DOM'],
    body:
      '브라우저가 화면에 그린 HTML 을 코드가 만질 수 있게 만들어 둔 나무 구조입니다. ' +
      '"버튼을 찾아 글자를 바꾼다", "새 줄을 끼워 넣는다" 같은 일이 모두 DOM 을 고치는 일이고, 이것이 웹에서 가장 느린 작업 축에 듭니다.',
  },
  {
    theme: '웹·브라우저',
    term: '렌더',
    en: 'render',
    match: ['렌더'],
    body:
      '데이터를 사람이 볼 수 있는 화면으로 그려 내는 일입니다. "다시 렌더한다"는 말은 화면을 새로 만들어 갈아 끼운다는 뜻이고, ' +
      '많은 양을 자주 다시 그리면 입력이 밀리는 느낌이 생깁니다.',
  },
  {
    theme: '웹·브라우저',
    term: '이벤트 · 핸들러',
    en: 'event / handler',
    match: ['이벤트'],
    body:
      '클릭·입력·스크롤처럼 "무슨 일이 일어났다"는 신호가 이벤트이고, 그 신호가 왔을 때 실행하라고 걸어 둔 함수가 핸들러입니다. ' +
      '웹 앱 코드의 상당 부분은 "무엇이 일어나면 무엇을 한다"는 이 연결의 모음입니다.',
  },
  {
    theme: '웹·브라우저',
    term: '비동기 · Promise',
    en: 'async / await',
    match: ['비동기', 'Promise'],
    body:
      '파일 읽기나 네트워크 요청처럼 오래 걸리는 일을 "기다리는 동안 화면을 멈추지 않고" 처리하는 방식입니다. ' +
      '결과가 나중에 온다는 약속표가 Promise 이고, async/await 는 그 기다림을 위에서 아래로 읽히게 적는 문법입니다. 순서가 눈에 보이는 대로 흐르지 않아 초급자가 가장 많이 헷갈리는 부분입니다.',
  },
  {
    theme: '웹·브라우저',
    term: 'localStorage',
    en: '브라우저 소형 저장소',
    match: ['localStorage'],
    body:
      '브라우저가 사이트마다 따로 들고 있는 작은 저장 공간으로, 문자열만 담기고 보통 5MB 안팎입니다. 창을 닫아도 남습니다. ' +
      '이 앱은 테마·최근 파일·설정 같은 가벼운 것만 여기 두고, 문서 내용처럼 큰 것은 IndexedDB 로 보냅니다.',
  },
  {
    theme: '웹·브라우저',
    term: 'IndexedDB',
    en: '브라우저 내장 데이터베이스',
    match: ['IndexedDB'],
    body:
      '브라우저 안에 들어 있는 큰 저장소로, 파일 덩어리까지 담을 수 있고 용량도 훨씬 큽니다. 대신 쓰는 법이 번거롭고 모든 작업이 비동기입니다. ' +
      '"브라우저 안"이라는 점이 중요합니다 — 사용자의 진짜 디스크가 아니라서, 브라우저 데이터를 지우면 같이 사라집니다.',
  },
  {
    theme: '웹·브라우저',
    term: 'iframe',
    en: '문서 안의 문서',
    match: ['iframe'],
    body:
      '페이지 안에 또 하나의 독립된 페이지를 끼워 넣는 요소입니다. 안쪽은 바깥과 어느 정도 격리되어 있어, 남의 문서를 안전하게 미리 보여 줄 때 씁니다.',
  },
  {
    theme: '웹·브라우저',
    term: 'contenteditable',
    en: '편집 가능 영역',
    match: ['contenteditable'],
    body:
      'HTML 요소에 이 속성을 주면 사용자가 그 자리에서 글을 고칠 수 있게 됩니다. 브라우저가 편집기를 공짜로 주는 셈이지만, ' +
      '붙여넣기·되돌리기·서식이 브라우저마다 제멋대로라 실제로는 손이 많이 갑니다.',
  },
  {
    theme: '웹·브라우저',
    term: 'fetch · XHR',
    en: '네트워크 요청',
    match: ['fetch', 'XHR'],
    body:
      '코드가 서버에 무언가를 요청하고 답을 받아 오는 방법입니다. XHR 이 옛 방식, fetch 가 요즘 방식입니다. ' +
      '이 앱에서는 주로 EXE 로컬 서버(127.0.0.1)에 저장·실행을 부탁할 때 씁니다.',
  },
  {
    theme: '웹·브라우저',
    term: '이스케이프 · 살균',
    en: 'escape / sanitize / XSS',
    match: ['이스케이프', '살균'],
    body:
      '사용자나 문서에서 온 글자를 화면에 넣기 전에 "글자로만 취급되도록" 무력화하는 처리입니다. ' +
      '이것을 빠뜨리면 문서 안에 숨겨 둔 코드가 진짜 코드로 실행될 수 있고(XSS), 이 앱처럼 남이 준 파일을 여는 프로그램에서는 특히 중요합니다.',
  },
  {
    theme: '웹·브라우저',
    term: 'SVG',
    en: 'Scalable Vector Graphics',
    match: ['SVG'],
    body:
      '점과 선의 좌표로 그림을 적어 두는 형식이라, 아무리 확대해도 깨지지 않고 글자로 검색도 됩니다. ' +
      '이 리뷰의 구조도들도 이미지 파일이 아니라 생성 시점에 만들어진 SVG 입니다.',
  },

  // ── EXE · 로컬 서버 · 네트워크 ────────────────────────────────────
  {
    theme: 'EXE·네트워크',
    term: 'EXE',
    en: '실행 파일',
    match: ['EXE'],
    body:
      '윈도우에서 더블클릭하면 바로 도는 프로그램 파일입니다. 이 앱의 EXE 는 화면을 직접 그리지 않고, ' +
      '작은 서버를 띄운 다음 브라우저로 앱 화면을 열어 줍니다. 그래서 브라우저 혼자서는 못 하는 일(진짜 디스크 저장, 로컬 Python 실행)이 가능해집니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '로컬 서버',
    en: 'local server',
    match: ['로컬 서버'],
    body:
      '인터넷 어딘가가 아니라 사용자 PC 안에서 도는 작은 서버입니다. 브라우저가 자기 컴퓨터에게 "이 파일 좀 저장해 줘"라고 부탁할 창구를 만들어 줍니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '포트',
    en: 'port',
    match: ['포트'],
    body:
      '한 컴퓨터 안에서 프로그램들을 구분하는 번호입니다(주소가 건물이면 포트는 호수). ' +
      '이 앱이 포트를 아무 번호나 쓰지 않고 정해진 후보 중에서 고르는 이유는, 브라우저 저장소가 "주소+포트"별로 따로 관리되어 번호가 바뀌면 저장한 것이 안 보이기 때문입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '127.0.0.1 · localhost',
    en: 'loopback',
    match: ['127.0.0.1', 'localhost', 'loopback'],
    body:
      '"이 컴퓨터 자신"을 가리키는 주소입니다. 이 주소로 보낸 요청은 랜선을 타지 않고 컴퓨터 안에서 되돌아옵니다. ' +
      '그래서 이 앱의 로컬 서버는 원칙적으로 바깥에서 접근할 수 없습니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '엔드포인트',
    en: 'endpoint',
    match: ['엔드포인트'],
    body:
      '서버에 부탁할 수 있는 일 하나하나에 붙은 주소입니다(/save-file, /run-python 처럼). ' +
      '"어떤 주소에 무엇을 보내면 무엇이 돌아온다"가 곧 클라이언트와 서버 사이의 계약입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: 'GET · POST',
    en: 'HTTP 메서드',
    match: ['POST', 'GET'],
    body:
      '요청의 종류입니다. GET 은 "달라"(주소창에 그대로 들어감), POST 는 "이걸 받아서 처리해"(내용을 본문에 담아 보냄)에 가깝습니다. ' +
      '저장처럼 상태를 바꾸는 일은 POST 로 두는 것이 관례입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: 'Origin · 교차 출처',
    en: 'origin / CORS',
    match: ['Origin', 'origin'],
    body:
      '"프로토콜 + 주소 + 포트" 세 쌍이 출처(origin)이고, 브라우저는 출처가 다르면 서로의 데이터에 손대지 못하게 막습니다. ' +
      '이 앱의 로컬 서버가 요청의 Origin 을 확인하는 것은, 다른 웹사이트가 사용자의 로컬 서버에 몰래 명령하지 못하게 하기 위해서입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: 'DNS rebinding',
    en: 'DNS 재바인딩 공격',
    match: ['rebinding'],
    body:
      '악의적인 웹사이트가 주소 이름을 중간에 127.0.0.1 로 바꿔치기해서, 사용자의 브라우저를 통해 그 사람 PC 안의 로컬 서버를 조종하려는 수법입니다. ' +
      '로컬 서버가 요청의 Host 헤더와 토큰을 함께 확인하는 이유가 이것입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '인증 토큰',
    en: 'auth token',
    match: ['토큰'],
    body:
      '"이 요청은 진짜 우리 앱이 보낸 것"임을 증명하는 임시 비밀 문자열입니다. 로컬 서버는 이 값이 맞는 요청만 처리합니다. ' +
      '주소만 알면 누구나 두드릴 수 있는 로컬 서버에서, 실제 방어선 역할을 합니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: 'heartbeat',
    en: '생존 신호',
    match: ['heartbeat'],
    body:
      '"나 아직 살아 있다"를 주기적으로 보내는 신호입니다. 이 앱은 브라우저 창이 닫혀 신호가 끊기면 로컬 서버도 스스로 종료해, ' +
      '보이지 않는 프로세스가 계속 남아 도는 것을 막습니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: 'LAN',
    en: '근거리 통신망',
    match: ['LAN'],
    body:
      '같은 공유기·같은 교실에 묶인 컴퓨터들의 망입니다. 이 앱의 시험지 배포처럼 "인터넷 없이 교실 안에서만" 주고받는 기능이 이 위에서 돕니다.',
  },

  // ── 빌드 · 테스트 · 배포 ─────────────────────────────────────────
  {
    theme: '빌드·테스트',
    term: '빌드',
    en: 'build',
    match: ['빌드'],
    body:
      '사람이 쓴 소스를 실제로 배포할 형태로 가공하는 과정입니다. 이 앱에서는 여러 파일을 하나의 오프라인 HTML 로 합치거나, ' +
      'C# 소스를 EXE 로 만드는 일이 여기 해당합니다.',
  },
  {
    theme: '빌드·테스트',
    term: '생성물',
    en: 'generated file',
    match: ['생성물', '자동 생성'],
    body:
      '사람이 쓴 것이 아니라 도구가 만들어 낸 파일입니다. 고쳐 봐야 다음 빌드에 덮어써지므로, 고쳐야 할 곳은 언제나 그것을 만들어 낸 원본입니다. ' +
      '이 리뷰의 review-data.generated.js 도 그런 파일입니다.',
  },
  {
    theme: '빌드·테스트',
    term: '런타임',
    en: 'runtime',
    match: ['런타임'],
    body:
      '"프로그램이 실제로 돌고 있는 동안"을 뜻합니다. 빌드할 때 잡히는 문제와 런타임에야 드러나는 문제는 성격이 다르고, ' +
      '이 프로젝트처럼 타입 검사가 없는 환경에서는 대부분의 문제가 런타임에 나타납니다.',
  },
  {
    theme: '빌드·테스트',
    term: 'npm · node',
    en: 'package.json',
    match: ['npm', 'node'],
    body:
      'node 는 브라우저 밖에서 자바스크립트를 돌리는 실행기이고, npm 은 라이브러리를 설치하고 정해 둔 명령을 실행해 주는 도구입니다. ' +
      'package.json 은 개발·빌드 의존성과 명령을 적어 둡니다. EXE의 JavaScript 라이브러리 기능은 이와 별도로 사용자가 고른 npm 패키지를 전용 캐시에 설치하고 Worker용 번들로 만듭니다.',
  },
  {
    theme: '빌드·테스트',
    term: 'esbuild',
    en: '빠른 번들러',
    match: ['esbuild'],
    body:
      '여러 자바스크립트 파일을 하나로 합치고 압축해 주는 도구입니다. 이 앱은 앱 본체가 아니라 일부 도구용으로만 씁니다.',
  },
  {
    theme: '빌드·테스트',
    term: '단위 테스트',
    en: 'unit test',
    match: ['단위 테스트'],
    body:
      '함수 하나처럼 작은 조각만 떼어 내, 정해진 입력에 정해진 결과가 나오는지 확인하는 자동 검사입니다. ' +
      '화면이 필요 없어 빠르게 많이 돌릴 수 있으므로, 순수한 계산 로직을 따로 떼어 두면 그만큼 테스트가 쉬워집니다.',
  },
  {
    theme: '빌드·테스트',
    term: 'E2E 테스트',
    en: 'end-to-end / Playwright',
    match: ['E2E', 'Playwright'],
    body:
      '진짜 브라우저를 자동으로 띄워 사람이 하듯 클릭하고 입력해 보는 검사입니다. 실제와 가장 가깝지만 느리고 잘 깨집니다. ' +
      'Playwright 가 그 도구이며, 이 앱에서는 "EXE 없는 상태"만 검사한다는 점이 중요합니다.',
  },
  {
    theme: '빌드·테스트',
    term: '계약 테스트',
    en: 'contract test',
    match: ['계약 테스트', 'release-contract'],
    body:
      '동작이 맞는지가 아니라 "목록끼리 어긋나지 않았는지"를 보는 검사입니다. ' +
      'HTML 의 스크립트 순서와 manifest 의 목록이 다르거나, 새 파일을 문서에 등록하지 않으면 실패합니다. 사람이 잊기 쉬운 등록 절차를 기계가 대신 지켜 줍니다.',
  },
  {
    theme: '빌드·테스트',
    term: '회귀',
    en: 'regression',
    match: ['회귀'],
    body:
      '잘 되던 기능이 다른 수정 때문에 다시 망가지는 것입니다. 테스트를 쌓아 두는 가장 큰 이유가 회귀를 자동으로 잡기 위해서입니다.',
  },
  {
    theme: '빌드·테스트',
    term: '커버리지',
    en: 'coverage',
    match: ['커버리지'],
    body:
      '테스트가 코드의 몇 퍼센트를 실제로 지나갔는지를 나타내는 수치입니다. 높다고 품질이 보장되지는 않지만(지나갔다고 확인한 것은 아니므로), ' +
      '아예 지나가지 않는 영역을 찾아내는 데는 유용합니다.',
  },
  {
    theme: '빌드·테스트',
    term: 'sha384 · 무결성',
    en: 'integrity',
    match: ['sha384', 'SHA-256', '무결성'],
    body:
      '파일 내용을 고정 길이 지문으로 요약한 값이 해시(sha384·SHA-256 은 그 방식들)이고, 그 지문을 미리 적어 두었다가 대조해 ' +
      '"내가 아는 그 파일이 맞는지" 확인하는 것이 무결성 검사입니다. 이 앱은 vendor 파일들이 몰래 바뀌지 않았는지 이렇게 확인하고, ' +
      'JDK·jar 를 내려받을 때도 배포처나 카탈로그에 적힌 SHA-256 과 대조한 뒤에만 씁니다.',
  },

  // ── 코드에서 자주 보는 개념 ──────────────────────────────────────
  {
    theme: '코드 개념',
    term: '파싱 · 파서',
    en: 'parse / parser',
    match: ['파싱', '파서'],
    body:
      '글자 덩어리를 의미 있는 구조로 해석해 내는 일입니다. CSV 한 줄을 칸으로 나누거나 XML 을 문서 구조로 바꾸는 것이 모두 파싱이며, ' +
      '이 앱처럼 남이 만든 파일을 여는 프로그램에서는 파싱이 코드의 큰 부분을 차지합니다.',
  },
  {
    theme: '코드 개념',
    term: '토크나이저',
    en: 'tokenizer',
    match: ['토크나이저'],
    body:
      '글자를 의미 단위(낱말·기호·문자열)로 잘게 끊는 단계로, 파싱의 앞부분입니다. ' +
      '이 리뷰의 코드 색칠도 외부 라이브러리 대신 직접 만든 토크나이저가 줄 단위로 처리합니다.',
  },
  {
    theme: '코드 개념',
    term: '정규식',
    en: 'regular expression',
    match: ['정규식'],
    body:
      '"이런 모양의 글자"를 기호로 적어 찾아내는 작은 언어입니다. 짧게 강력하지만 읽기 어렵고, ' +
      '모양이 조금만 벗어나도 놓치거나 엉뚱한 것까지 잡아서 오검출의 단골 원인이 됩니다.',
  },
  {
    theme: '코드 개념',
    term: '직렬화 · 역직렬화',
    en: 'serialize / deserialize',
    match: ['직렬화'],
    body:
      '메모리 안의 데이터를 저장하거나 보낼 수 있는 한 줄짜리 형태(주로 JSON 문자열)로 바꾸는 것이 직렬화, 그것을 다시 데이터로 되살리는 것이 역직렬화입니다. ' +
      '저장·전송 기능이 있는 곳에는 거의 항상 이 한 쌍이 있습니다.',
  },
  {
    theme: '코드 개념',
    term: '해시',
    en: 'hash',
    match: ['해시'],
    body:
      '아무리 큰 내용이라도 고정 길이의 짧은 값으로 요약해 주는 계산입니다. 내용이 한 글자만 달라도 값이 완전히 달라지므로 ' +
      '"같은 파일인가"를 빠르게 비교하는 데 씁니다. 반대로 값에서 원본을 되돌릴 수는 없습니다.',
  },
  {
    theme: '코드 개념',
    term: '캐시',
    en: 'cache',
    match: ['캐시'],
    body:
      '한 번 계산하거나 받아 온 결과를 저장해 두고 다음에 다시 쓰는 것입니다. 빨라지는 대신 "원본이 바뀌었는데 옛 값을 계속 보여 주는" 문제가 따라옵니다. ' +
      '브라우저가 수정된 js·css 를 즉시 반영하지 않아 강제 새로고침이 필요한 것도 같은 이유입니다.',
  },
  {
    theme: '코드 개념',
    term: '스냅샷',
    en: 'snapshot',
    match: ['스냅샷'],
    body:
      '어느 시점의 상태를 통째로 찍어 둔 사본입니다. 되돌리기(undo)나 자동 복구는 대개 스냅샷을 쌓아 두었다가 되돌려 놓는 방식으로 만듭니다.',
  },
  {
    theme: '코드 개념',
    term: 'diff',
    en: '차이 비교',
    match: ['diff'],
    body:
      '두 버전의 글을 견주어 어디가 추가·삭제·수정되었는지 찾아내는 계산입니다. ' +
      '같은 결과라도 어떤 알고리즘을 쓰느냐에 따라 사람이 보기에 자연스러운 정도가 달라집니다.',
  },
  {
    theme: '코드 개념',
    term: 'undo · redo 스택',
    en: '되돌리기 이력',
    match: ['undo', 'redo', '되돌리기'],
    body:
      '되돌리기와 다시 실행을 위해 변경 이력을 쌓아 두는 구조입니다. 되돌린 뒤 새 편집을 하면 앞서 되돌려 둔 이력은 버려지는 것이 보통이라, ' +
      '이 경계를 잘못 다루면 "되돌리기를 눌렀더니 엉뚱한 상태가 되는" 버그가 납니다.',
  },
  {
    theme: '코드 개념',
    term: '큐 · 락',
    en: 'queue / lock / mutex',
    match: ['큐', '락', '뮤텍스'],
    body:
      '여러 작업이 한 자원을 동시에 건드리지 못하게 줄을 세우거나(큐), 한 번에 하나만 들어가도록 잠그는 장치(락·뮤텍스)입니다. ' +
      '저장이 겹쳐 파일이 반쯤 섞이는 사고를 막는 것이 대표적인 쓰임입니다.',
  },
  {
    theme: '코드 개념',
    term: '경쟁 상태',
    en: 'race condition',
    match: ['경쟁', '레이스'],
    body:
      '두 작업의 순서가 상황에 따라 달라져 결과가 들쭉날쭉해지는 문제입니다. 평소엔 멀쩡하다가 느린 PC 나 큰 파일에서만 터지는 것이 특징이라 ' +
      '재현과 추적이 가장 까다로운 부류에 듭니다.',
  },
  {
    theme: '코드 개념',
    term: '워커',
    en: 'Web Worker',
    match: ['워커', 'Worker'],
    body:
      '화면을 담당하는 흐름과 별도로 도는 일꾼입니다. 무거운 계산을 워커에 맡기면 그동안에도 화면이 멈추지 않습니다. ' +
      '대신 워커는 화면(DOM)을 직접 만질 수 없어 메시지로만 주고받습니다. ' +
      '리뷰에서는 브라우저의 Web Worker(JavaScript 실행기)와 런처가 띄우는 상주 프로세스(DB 워커)를 모두 이 말로 부릅니다.',
  },

  // ── 파일 형식과 데이터 ───────────────────────────────────────────
  {
    theme: '파일 형식',
    term: 'OOXML',
    en: 'docx · pptx · xlsx의 속',
    // XML 자체는 바로 아래의 일반 XML 항목이 맡는다.
    match: ['docx', 'pptx'],
    body:
      'Word·PowerPoint·Excel 의 요즘 파일은 사실 XML 문서 여러 개를 ZIP 으로 묶어 둔 것입니다. 확장자만 zip 으로 바꿔 풀면 안이 보입니다. ' +
      '이 앱이 문서를 "다시 만들지 않고 필요한 부분만 바꿔 넣을" 수 있는 것도 이 구조 덕분입니다.',
  },
  {
    theme: '파일 형식',
    term: 'XML',
    en: '태그로 감싼 데이터',
    match: ['XML'],
    body:
      'HTML 처럼 꺾쇠 태그로 내용을 감싸 구조를 표현하는 형식입니다. 사람이 읽기엔 장황하지만 규칙이 엄격해 기계가 다루기 좋습니다.',
  },
  {
    theme: '파일 형식',
    term: 'JSON',
    en: 'JavaScript Object Notation',
    match: ['JSON'],
    body:
      '중괄호와 대괄호로 데이터를 적는 가장 흔한 형식입니다. 사람이 읽을 수 있고 거의 모든 언어가 다룰 수 있어, 설정 파일과 통신에 널리 쓰입니다.',
  },
  {
    theme: '파일 형식',
    term: 'YAML',
    en: '들여쓰기로 쓰는 설정',
    match: ['YAML'],
    body:
      'JSON 과 담는 내용은 비슷하지만 괄호 대신 들여쓰기로 구조를 표현해 사람이 쓰기 편한 형식입니다. 대신 들여쓰기 한 칸에 의미가 달라집니다.',
  },
  {
    theme: '파일 형식',
    term: 'CSV · TSV',
    en: '쉼표·탭 구분 표',
    match: ['CSV', 'TSV'],
    body:
      '표를 글자로만 적은 가장 단순한 형식으로, 칸을 쉼표(CSV)나 탭(TSV)으로 나눕니다. ' +
      '단순해 보여도 값 안에 쉼표나 줄바꿈이 들어가는 경우 때문에 제대로 읽는 일은 생각보다 까다롭습니다.',
  },
  {
    theme: '파일 형식',
    term: 'Markdown',
    en: '기호로 쓰는 서식',
    match: ['Markdown'],
    body:
      '# 이나 * 같은 기호 몇 개로 제목·목록·강조를 적는 가벼운 문법입니다. 글자 그대로도 읽히고 HTML 로 바꾸기도 쉬워 문서에 널리 쓰입니다.',
  },
  {
    theme: '파일 형식',
    term: 'SQLite',
    en: '파일 하나짜리 데이터베이스',
    match: ['SQLite'],
    body:
      '서버를 따로 띄우지 않고 파일 한 개가 곧 데이터베이스인 방식입니다. 설치가 필요 없어 앱 안에 품고 쓰기 좋습니다.',
  },
  {
    theme: '파일 형식',
    term: 'HWP · HWPX',
    en: '한글 문서',
    match: ['HWP'],
    body:
      '한글 워드프로세서의 문서 형식입니다. HWPX 는 XML 기반이라 다루기가 비교적 수월하지만, 옛 HWP 는 구조가 복잡한 이진 형식이라 읽기가 훨씬 어렵습니다.',
  },
  {
    theme: '파일 형식',
    term: '자막 형식',
    en: 'SRT · VTT · SMI',
    match: ['SRT', 'VTT', 'SMI'],
    body:
      '"몇 초부터 몇 초까지 이 말이 나온다"를 적어 둔 글자 파일입니다. 셋은 적는 방식만 다를 뿐 담는 내용은 같아서 서로 변환할 수 있습니다.',
  },
  {
    theme: '파일 형식',
    term: 'ipynb',
    en: 'Jupyter 노트북',
    match: ['ipynb', 'notebook'],
    body:
      '코드와 설명, 실행 결과를 한 문서에 칸(셀) 단위로 담아 두는 형식입니다. 속은 JSON 이라 프로그램이 읽고 고치기 쉽습니다.',
  },
  {
    theme: '파일 형식',
    term: 'Pyodide',
    en: '브라우저 속 Python',
    match: ['Pyodide'],
    body:
      'Python 을 WebAssembly(브라우저가 돌릴 수 있는 저수준 실행 형식)로 바꿔 넣은 것으로, 설치 없이 브라우저 안에서 Python 을 실행하게 해 줍니다. ' +
      '대신 용량이 크고 처음 불러올 때 시간이 걸립니다.',
  },
  {
    theme: '파일 형식',
    term: '인코딩 · UTF-8 · BOM',
    en: 'encoding',
    match: ['인코딩', 'BOM'],
    body:
      '글자를 컴퓨터가 저장하는 숫자로 바꾸는 약속입니다. 약속이 어긋나면 한글이 깨져 보입니다. UTF-8 이 표준이고, ' +
      'BOM 은 파일 맨 앞에 붙는 "이 파일은 이 방식입니다"라는 몇 바이트짜리 표식인데, 있고 없고에 따라 프로그램이 다르게 반응해 종종 말썽이 됩니다.',
  },

  // ── 2026-08-20 추가: 지도·칠판 확장으로 리뷰에 자주 나오게 된 말 ──────────
  // 앞의 항목들과 같은 기준으로 골랐다 — 리뷰 산문에서 실제로 자주 나오면서, 뜻을 모르면
  // 문장 전체를 놓치는 말만. '상한'·'오프라인'처럼 자주 나오지만 평범한 한국어인 것은 뺐다.
  {
    theme: '코드 개념',
    term: '순수 함수',
    en: 'pure function',
    match: ['순수 함수', '순수 코어', '순수부'],
    body:
      '같은 값을 넣으면 언제나 같은 값이 나오고, 그 밖의 것은 아무것도 건드리지 않는 함수입니다. 화면을 고치지도, 파일을 쓰지도, 시각을 읽지도 않습니다. ' +
      '이 리뷰에서 이 말이 자주 나오는 이유는 그것이 곧 "테스트할 수 있는가"와 같은 뜻이기 때문입니다 — 브라우저도 가짜 객체도 없이 값만 넣어 보면 되니까요. ' +
      '이 앱에서는 음악 규칙(music-model.js), 형식 변환(data-convert.js), 수식 엔진, 칠판 계산 도구가 이 방식으로 떼어져 있습니다.',
  },
  {
    theme: '코드 개념',
    term: '벡터 항목',
    en: 'vector item',
    match: ['벡터 항목', '벡터'],
    body:
      '그림을 픽셀 덩어리(사진)가 아니라 "어디서 어디까지 선, 반지름 얼마인 원" 같은 좌표와 수치로 들고 있는 방식입니다. ' +
      '이 앱의 칠판이 그린 것을 이렇게 저장하기 때문에 나중에 색을 바꾸고, 확대해도 안 깨지고, 묶음을 풀어 낱개로 고칠 수 있습니다. ' +
      '사진으로 저장했다면 셋 다 불가능했을 것이고, 이것이 도구상자 계산부를 따로 떼어 둘 수 있었던 이유이기도 합니다.',
  },
  {
    theme: '웹·브라우저',
    term: '도구상자',
    en: 'toolbox',
    match: ['도구상자'],
    body:
      '칠판(화이트보드) 옆에 떠 있는 창으로, 기호·수식·도형·과학 스텐실과 함수 그래프·자료 차트·주기율표 같은 것을 골라 넣는 곳입니다. ' +
      '이 리뷰에서 이 말이 자주 나오는 이유는 그 안의 계산이 화면 코드에서 분리돼 board-tools.js 라는 별도 모듈이 됐기 때문입니다 — ' +
      '"도구상자"라고 하면 보통 그 계산부를 가리킵니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '프록시',
    en: 'proxy',
    match: ['프록시'],
    body:
      '내가 직접 남의 서버를 부르는 대신 중간에 누군가가 대신 받아다 주는 방식입니다. 이 앱에서는 지도 배경 그림을 브라우저가 직접 받지 않고 ' +
      'EXE 런처가 대신 받아 옵니다. 그렇게 하면 ① 어느 주소를 부를 수 있는지 한 곳에서 막을 수 있고 ② 받아 온 것을 디스크에 남겨 두었다가 ' +
      '인터넷이 없는 교실에서도 다시 쓸 수 있습니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '타일',
    en: 'tile',
    match: ['타일'],
    body:
      '지도 그림은 한 장이 아니라 256픽셀짜리 정사각형 조각 여러 개를 바둑판처럼 이어 붙인 것입니다. 그 조각 하나를 타일이라고 합니다. ' +
      '화면을 옮기거나 확대할 때마다 필요한 조각만 새로 받아 오기 때문에, "지도를 받는다"는 것은 실제로는 "보이는 타일만 받는다"는 뜻입니다. ' +
      '이 앱이 지도를 미리 통째로 받아 두지 않는 것도 이 때문입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '지오코딩',
    en: 'geocoding',
    match: ['지오코딩', '되묻기'],
    body:
      '주소나 장소 이름을 위도·경도 숫자로 바꾸는 일입니다("세종대로 110" → 37.566, 126.978). 반대로 좌표를 주소로 바꾸는 것은 역지오코딩이라 하고, ' +
      '이 리뷰에서는 "좌표를 주소로 되묻는다"고 표현합니다. 둘 다 바깥 서비스에 물어봐야 하는 일이라, 이 앱에서는 EXE 런처를 거쳐서만 부릅니다.',
  },
  {
    theme: '파일 형식',
    term: '마커 · 표시',
    en: 'marker',
    match: ['마커'],
    body:
      '지도 위에 찍는 핀 하나입니다. 이 앱의 화면 문구에서는 "표시"라고 부르고 코드에서는 marker 라고 씁니다 — 같은 것입니다. ' +
      '각각 좌표·이름·메모·색을 갖고, 사진을 한 장 붙일 수도 있습니다. .map 파일에 저장되는 알맹이가 사실상 이 목록입니다.',
  },
  {
    theme: '코드 개념',
    term: '좌표계 · 좌표',
    en: 'coordinates',
    match: ['좌표'],
    body:
      '"어디인가"를 숫자 짝으로 적는 방식입니다. 이 리뷰에는 두 종류가 섞여 나옵니다 — 지도의 위도·경도(지구 위의 자리)와 ' +
      '화면의 x·y 픽셀(창 안의 자리)입니다. 둘을 서로 바꾸는 계산이 지도 코드의 상당 부분이고, ' +
      '칠판에서는 또 다른 구분이 있습니다(판서 좌표 대 화면 좌표) — 확대해도 스포트라이트가 제자리에 머무는 것이 그 덕분입니다.',
  },

  // ── 원격 터미널이 들어오면서 늘어난 말 ──────────────────────────────
  {
    theme: 'EXE·네트워크',
    term: 'SSH',
    en: 'secure shell',
    match: ['SSH', 'OpenSSH'],
    body:
      '다른 컴퓨터에 안전하게 접속해 그 컴퓨터의 명령창을 쓰는 방식입니다. 리눅스 서버를 다룰 때 가장 흔히 쓰는 길이고, ' +
      '주고받는 내용이 모두 암호화됩니다. 이 앱은 SSH 를 직접 구현하지 않고 Windows 에 딸려 오는 OpenSSH 클라이언트(ssh.exe)를 대신 실행합니다 — ' +
      '암호 협상처럼 틀리면 곧바로 보안 사고가 되는 부분을 스스로 지지 않으려는 선택입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: 'PTY · ConPTY',
    en: 'pseudo terminal',
    match: ['PTY', 'ConPTY'],
    body:
      '프로그램에게 "지금 진짜 터미널 앞에 있다"고 믿게 해 주는 가짜 터미널입니다. 이것이 있어야 vim 처럼 화면 전체를 그리는 프로그램, ' +
      '방향키, Ctrl+C, 창 크기 변경이 제대로 동작합니다. 단순히 명령의 출력만 받아 오는 것과 결정적으로 다른 지점입니다. ' +
      'ConPTY 는 Windows 10 1809 부터 들어간 그 기능의 이름이고, 이 앱은 그 API 가 있는지를 버전 대신 함수 존재 여부로 확인합니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '호스트 키 지문 · known_hosts',
    en: 'host key fingerprint',
    match: ['지문', 'known_hosts'],
    body:
      '접속하려는 서버가 "정말 그 서버가 맞는지" 확인하는 값입니다. 서버마다 공개키가 있고, 그것을 짧게 줄인 것이 지문입니다. ' +
      '한 번 확인한 지문은 known_hosts 라는 목록에 적어 두고, 다음부터는 그 목록과 맞는 서버에만 붙습니다 — 중간에서 남이 가로채는 것을 막는 장치입니다. ' +
      '이 앱은 사용자가 관리자에게 받은 값과 눈으로 맞춰 본 뒤에만 목록에 넣고, 목록에 없으면 아예 붙지 않습니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '폴링',
    en: 'polling',
    match: ['폴링', '증분'],
    body:
      '"새로 온 것 있나요?"를 짧은 간격으로 되묻는 방식입니다. 서버가 먼저 밀어 주는 방식(WebSocket 등)의 반대편이고, ' +
      '이 앱은 파이썬 실행·노트북 커널·PowerShell 터미널·SSH 가 모두 폴링을 씁니다. ' +
      '매번 전부 받지 않고 "지금까지 받은 데까지"를 오프셋으로 함께 보내 그 뒤 늘어난 부분만 받는 것이 요령입니다.',
  },
  {
    theme: '코드 개념',
    term: 'base64',
    en: 'base64',
    match: ['base64'],
    body:
      '그림·소리 같은 이진 데이터를 글자만으로 적는 방법입니다. 대신 크기가 3분의 4쯤으로 불어납니다. ' +
      '이 앱에서는 사진을 JSON 문서 안에 그대로 담을 때(.map 의 표시 사진, .timeline 의 사건 사진)와 ' +
      '터미널 출력 바이트를 JSON 응답에 실을 때 씁니다. 문서 크기 상한을 글자 수로 세는 것도 이 때문입니다.',
  },
  {
    theme: '코드 개념',
    term: '기원전 · 천문학적 연도',
    en: 'astronomical year numbering',
    match: ['기원전'],
    body:
      '기원전 300년처럼 0년보다 앞선 해를 컴퓨터가 정렬할 수 있게 하나의 수로 적는 방식입니다. ' +
      '기원전 1년을 0, 기원전 2년을 -1 로 두면 기원전과 기원후가 끊기지 않는 하나의 축이 됩니다. ' +
      '연대표가 별도 달력 라이브러리 없이 "기원전 300"과 "2026-08-20"을 같은 자로 정렬하는 것이 이 환산 덕분입니다.',
  },

  // ── 2026-08-31 추가: 작업공간·관계도·환율·악보 확장으로 자주 나오게 된 말 ──
  // 앞의 항목들과 같은 기준으로 골랐다 — 자주 나오면서 뜻을 모르면 문장을 놓치는 말만.
  // '상한'(68)·'복습'(12)·'빈칸'(12)처럼 자주 나와도 평범한 한국어인 것은 뺐다.
  {
    theme: '뼈대',
    term: '작업공간',
    en: 'workspace',
    match: ['작업공간'],
    body:
      '이 리뷰에서 이 말은 두 가지를 가리키므로 나눠 읽어야 합니다. ' +
      '① 가상 작업공간 — 열려 있는 문서들을 여러 묶음으로 갈라 화면마다 다른 탭·배치를 두는 기능입니다. ' +
      '파일을 복사하지 않고 "어느 묶음이 어느 문서를 갖는가"만 저장합니다. ' +
      '② 작업공간 저장소(workspace.bin) — 앱을 껐다 켜도 열려 있던 파일이 되살아나게 하는 자동 복원용 원본 보관소입니다. ' +
      '①을 지워도 ②의 파일은 남습니다. 이름이 같아 헷갈리기 쉬운 자리라 코드에서도 첫 주석이 이 구분을 먼저 밝힙니다.',
  },
  {
    theme: '코드 개념',
    term: '정규화',
    en: 'normalize',
    match: ['정규화'],
    body:
      '들어온 값을 "믿을 수 있는 한 가지 모양"으로 다듬어 놓는 일입니다. 빠진 칸은 기본값으로 채우고, 범위를 벗어난 수는 잘라 넣고, ' +
      '모르는 값은 버립니다. 이 앱은 사람이 손으로 고칠 수 있는 JSON 파일(.map·.concept·.study …)을 열고 ' +
      '남의 서버가 준 응답도 그대로 받으므로, 화면 코드가 그 값을 만지기 전에 반드시 한 번 거치는 관문입니다. ' +
      '"이상한 파일이 들어와도 화면이 깨지지 않는다"는 이 앱의 성질이 대부분 여기서 나옵니다.',
  },
  {
    theme: '코드 개념',
    term: '색인',
    en: 'index',
    match: ['색인', '인덱스'],
    body:
      '"찾을 것"과 "찾은 자리"를 미리 짝지어 표로 만들어 두는 것입니다. 목록을 처음부터 훑으면 개수만큼 시간이 걸리지만, ' +
      '색인이 있으면 몇 개가 있든 한 번에 찾습니다. 이 앱은 열린 문서를 경로·핸들로, 사이드바 항목을 id 로, ' +
      'Python 소스를 import 이름으로 색인해 둡니다 — 파일 수천 개짜리 폴더를 여는 상황을 염두에 둔 것입니다.',
  },
  {
    theme: '웹·브라우저',
    term: '캔버스',
    en: 'canvas',
    match: ['캔버스'],
    body:
      '이 리뷰에서 두 뜻으로 쓰입니다. ① HTML 의 <canvas> — 픽셀을 직접 찍어 그리는 그림판 요소로, 화면을 PNG 로 굳힐 때 씁니다. ' +
      '② 관계도·화이트보드의 "캔버스" — 카드와 그림이 놓이는 넓은 판을 가리키는 말로, 화면보다 크고 확대·이동해 가며 봅니다. ' +
      '앞의 것은 브라우저 기능이고 뒤의 것은 이 앱이 만든 개념입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '세션',
    en: 'session',
    match: ['세션'],
    body:
      '"한 번 시작해서 끝날 때까지 이어지는 하나의 일"과, 그 일이 진행되는 동안 서버가 들고 있는 상태를 함께 가리킵니다. ' +
      'Python 실행 세션, 노트북 커널 세션, PowerShell 터미널 세션, SSH 세션이 모두 이 꼴입니다 — ' +
      '열기(open) → 입력·폴링 → 끊기(stop)의 세 걸음을 갖고, 각각 id 로 구분되며, 끊지 않으면 서버에 남습니다. ' +
      '그래서 이 앱은 세션마다 동시 개수와 출력 버퍼 상한을 따로 둡니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: '고시환율 · 매매기준율',
    en: 'published rate / base rate',
    match: ['고시환율', '매매기준율', '고시'],
    body:
      '은행이 "오늘은 이 값으로 봅니다" 하고 공표하는 환율이 고시환율이고, 그중 사고팔기의 기준이 되는 값이 매매기준율입니다. ' +
      '실제로 송금할 때는 여기에 수수료가 붙어 "보낼 때"와 "받을 때" 값이 따로 있습니다. ' +
      '엔화처럼 100단위로 고시하는 통화가 있어, 표에 보이는 수와 1단위 환산값을 구분해 들고 있어야 계산이 맞습니다.',
  },
  {
    theme: '파일 형식',
    term: '이조 악기 · 파트',
    en: 'transposing instrument / part',
    match: ['이조', '파트'],
    body:
      '파트는 합주에서 악기 하나가 맡는 줄입니다. 이조 악기는 "적힌 음과 실제로 울리는 음이 다른" 악기로, ' +
      'B♭ 클라리넷은 악보의 도를 불면 실제로는 시♭ 이 납니다. 그래서 같은 곡이라도 파트보마다 조를 옮겨 적어야 하는데, ' +
      '이 앱은 그 옮겨 적기를 자동으로 합니다 — 밴드부에서 손으로 하던 일입니다.',
  },

  // ── 데이터베이스: DB 클라이언트를 읽을 때 필요한 말 ────────────────────
  {
    theme: '데이터베이스',
    term: '스키마',
    en: 'schema',
    match: ['스키마'],
    body:
      '데이터베이스 안에서 테이블·뷰 같은 것들을 담아 두는 이름 공간입니다. MySQL 에서는 데이터베이스와 사실상 같은 말로 쓰입니다. ' +
      '"스키마를 읽는다" 는 데이터를 가져오는 것이 아니라 "여기에 어떤 테이블이 있고 각 테이블에 어떤 칸이 있는가" 라는 지도를 가져오는 일입니다.',
  },
  {
    theme: '데이터베이스',
    term: '테이블 · 뷰',
    en: 'table / view',
    match: ['테이블', '뷰'],
    body:
      '테이블은 실제로 값이 들어 있는 표이고, 뷰는 "이 질의 결과를 표처럼 보이게 해 둔 이름" 입니다. ' +
      '뷰에는 값이 저장돼 있지 않으므로 이 앱도 뷰의 칸은 고치지 못하게 잠급니다 — 어느 테이블의 어느 행을 고쳐야 할지 정할 수 없기 때문입니다.',
  },
  {
    theme: '데이터베이스',
    term: '기본키',
    en: 'primary key',
    match: ['기본키'],
    body:
      '표에서 행 하나를 다른 행과 구별해 짚을 수 있게 해 주는 칸(또는 칸 묶음)입니다. ' +
      '이 앱이 표의 값을 고칠 때 조건으로 쓰는 것이 언제나 기본키입니다. 그래서 기본키가 결과에 실려 있지 않으면 ' +
      '"어느 행을 고칠지" 를 말할 수 없어 편집이 잠깁니다.',
  },
  {
    theme: '데이터베이스',
    term: '트랜잭션',
    en: 'transaction',
    match: ['트랜잭션'],
    body:
      '여러 개의 변경을 "전부 되거나 전부 안 되거나" 로 묶는 단위입니다. 중간에 하나가 실패하면 앞의 것들도 없던 일이 됩니다. ' +
      '이 앱이 담아 둔 변경을 한 묶음으로 적용할 때 쓰는 것이 이것이고, 덕분에 절반만 반영된 상태가 남지 않습니다.',
  },
  {
    theme: '데이터베이스',
    term: '커밋 · 롤백',
    en: 'commit / rollback',
    match: ['커밋', '롤백'],
    body:
      '커밋은 트랜잭션 안에서 한 일을 확정하는 것이고, 롤백은 없던 일로 되돌리는 것입니다. ' +
      '롤백은 확정하기 전에만 쓸 수 있습니다 — 이미 커밋된 것은 이 명령으로 돌아오지 않습니다.',
  },
  {
    theme: '데이터베이스',
    term: '자동 커밋',
    en: 'autocommit',
    match: ['자동 커밋'],
    body:
      '문장 하나가 끝날 때마다 자동으로 확정하는 모드입니다. 켜 두면 실수한 UPDATE 를 되돌릴 수 없고, ' +
      '끄면 되돌릴 수는 있지만 트랜잭션이 계속 열려 있어 서버가 옛 값을 붙들고 있게 됩니다. ' +
      '그래서 이 앱은 켠 것을 기본으로 두고, 끄는 것은 사용자가 그때그때 고르는 스위치로 뒀습니다.',
  },
  {
    theme: '데이터베이스',
    term: 'DDL',
    en: 'data definition language',
    match: ['DDL'],
    body:
      'CREATE·ALTER·DROP 처럼 "무엇이 있는지" 자체를 바꾸는 문장들입니다(값을 바꾸는 INSERT·UPDATE 와 구분됩니다). ' +
      'MySQL 에서는 이런 문장이 실행되는 순간 앞의 트랜잭션까지 함께 확정되므로(암묵적 커밋), 그 뒤에는 롤백해도 돌아오지 않습니다.',
  },
  {
    theme: '데이터베이스',
    term: '드라이버',
    en: 'driver',
    match: ['드라이버'],
    body:
      '어떤 언어에서 특정 데이터베이스와 말을 주고받게 해 주는 라이브러리입니다. 이 앱은 파이썬용 MySQL 드라이버(pymysql)를 씁니다. ' +
      '드라이버가 없으면 접속 자체가 불가능하므로, 앱이 그 사실을 먼저 확인하고 설치 버튼을 보여 줍니다.',
  },
  {
    theme: '데이터베이스',
    term: '자리표시자',
    en: 'placeholder / parameter binding',
    match: ['자리표시자'],
    body:
      'SQL 문장에 값을 글자로 이어 붙이지 않고 빈자리(%s)만 남긴 뒤, 값은 따로 드라이버에 넘기는 방식입니다. ' +
      "따옴표가 든 이름(O'Brien)이나 NULL·날짜가 문장을 망가뜨리지 않고, 남이 넣은 값이 명령으로 해석되는 사고(SQL 주입)도 막힙니다. " +
      '이 앱이 "앱은 SQL 을 짓지 않는다" 를 지킬 수 있는 것이 이 방식 덕분입니다.',
  },
  {
    theme: '데이터베이스',
    term: '덤프',
    en: 'dump',
    match: ['덤프'],
    body:
      '데이터베이스의 구조와 내용을 다시 만들 수 있는 SQL 문장들로 뽑아 파일 하나에 적어 두는 것입니다. ' +
      '백업이자 다른 서버로 옮기는 수단이고, 이 앱에서는 "고치고 지울 수 있는 도구를 주었으니 되돌릴 수단도 함께 준다" 는 이유로 붙었습니다.',
  },
  {
    theme: '데이터베이스',
    term: 'SFTP',
    en: 'SSH File Transfer Protocol',
    match: ['SFTP'],
    body:
      'SSH 연결 위에서 파일을 주고받는 규약입니다. 이름은 FTP 를 닮았지만 전혀 다른 것으로, SSH 가 이미 맺어 준 안전한 통로를 그대로 씁니다. ' +
      '이 앱의 원격 파일 기능은 셸 명령을 보내지 않고 이 규약의 읽기 동작만 씁니다 — 그래서 원격 파일을 고치거나 실행할 길이 아예 없습니다.',
  },

  // ── 2026-09-13 추가 ────────────────────────────────────────────────
  // id(term-N)가 목록 순서로 매겨지므로 새 항목은 끝에 붙인다. 화면에서는 theme 별로 모여 보인다.
  // 자바 실습·실시간 교통·경로 방식 영상이 들어오며 산문이 14% 늘었고, 빈도를 다시 세어 고른 것들이다
  // (채점 70 · 자동완성 39 · Worker 39 · 컴파일 29 · JDK 24 · 스레드 11 …). 상한(83)·진단(33)·실측(11)은
  // 자주 나오지만 평범한 한국어로 읽혀 넣지 않았다.

  // ── 코드 실행: 자바 실습·채점을 읽을 때 필요한 말 ─────────────────────
  {
    theme: '코드 실행',
    term: 'JDK',
    en: 'Java Development Kit',
    match: ['JDK', 'JRE'],
    body:
      '자바 코드를 컴파일하고 실행하는 도구 묶음입니다. 실행만 하는 JRE 와 달리 컴파일러(javac)가 들어 있습니다. ' +
      '브라우저 안에는 자바 실행기가 없어서 이 앱의 자바 실습은 EXE 가 이 PC 에서 찾은(또는 대신 내려받은) JDK 로만 돌고, JRE 만 있는 PC 는 JDK 로 치지 않습니다.',
  },
  {
    theme: '코드 실행',
    term: '컴파일 · javac',
    en: 'compile',
    match: ['컴파일', 'javac'],
    body:
      '사람이 쓴 소스를 컴퓨터가 실행할 형태로 미리 번역하는 단계입니다. javac 는 자바의 컴파일러입니다. ' +
      '파이썬은 이 단계 없이 바로 돌지만 자바는 컴파일이 먼저라, 오타 같은 문법 오류가 실행 전에 "컴파일 오류" 로 한꺼번에 나옵니다. 이 앱이 저장할 때 javac 만 돌려 보는 저장 검사가 이것입니다.',
  },
  {
    theme: '코드 실행',
    term: 'jar · 클래스패스',
    en: 'jar / classpath',
    match: ['jar', '클래스패스', '-cp'],
    body:
      'jar 는 컴파일된 자바 코드를 묶어 둔 압축 파일이고, 클래스패스는 "자바가 클래스를 찾아볼 폴더·jar 목록" 입니다. ' +
      '라이브러리를 쓰려면 그 jar 를 클래스패스에 올려야 합니다. 이 앱은 고른 jar 를 컴파일과 실행 양쪽에 같은 목록으로 넘깁니다 — 한쪽에만 주면 import 부터 실패합니다.',
  },
  {
    theme: '코드 실행',
    term: 'Maven Central',
    en: 'Maven Central repository',
    match: ['Maven'],
    body:
      '자바 라이브러리가 모여 있는 공개 저장소입니다. 라이브러리는 group:artifact:version(예: com.google.code.gson:gson:2.11.0) 좌표로 가리킵니다. ' +
      '보통은 Maven 같은 빌드 도구가 딸린 라이브러리까지 알아서 받지만, 이 앱은 그 도구를 들이지 않고 jar 파일 하나로 끝나는 것만 받습니다.',
  },
  {
    theme: '코드 실행',
    term: '표준입력 · 파이프',
    en: 'stdin / pipe',
    match: ['표준입력', '파이프', 'stdin'],
    body:
      '실행 중인 프로그램이 키보드 입력을 받는 통로가 표준입력이고, 그 통로에 다른 프로그램이 글자를 흘려 넣도록 이어 둔 관이 파이프입니다. ' +
      '이 앱의 대화형 실행은 입력 칸에 친 한 줄을 파이프로 넘기고, 채점은 테스트 입력 전체를 한 번에 흘려 넣고 닫습니다. 프로그램이 입력을 기다리는데 아무도 안 넣어 주면 멈춘 것처럼 보입니다.',
  },
  {
    theme: '코드 실행',
    term: '식별자',
    en: 'identifier',
    match: ['식별자'],
    body:
      '변수·함수·클래스 이름처럼 코드 안에서 무언가를 가리키는 이름입니다. 언어마다 쓸 수 있는 글자 규칙이 있습니다. ' +
      '자바는 파일 이름이 곧 public 클래스 이름이어야 해서, 이 앱은 파일 이름을 식별자 규칙으로 검사하고, 경로를 받지 않고 식별자만 받아 폴더를 만들어 임시 폴더 밖으로 새지 않게 합니다.',
  },
  {
    theme: '코드 실행',
    term: '채점 · 자동채점',
    en: 'auto grading',
    match: ['채점'],
    body:
      '선생님이 적어 둔 입력을 학생 코드에 넣어 돌리고, 나온 출력을 기대한 출력과 비교해 맞았는지 가리는 기능입니다. ' +
      '이 앱은 파이썬·자바스크립트·자바가 같은 테스트 모양과 같은 판정 순서를 쓰고, 테스트마다 새로 실행해 앞 테스트가 남긴 상태가 다음 테스트에 섞이지 않게 합니다.',
  },
  {
    theme: '코드 실행',
    term: '자동완성',
    en: 'autocomplete',
    match: ['자동완성'],
    body:
      '코드를 치는 도중 이어서 올 수 있는 이름을 목록으로 보여 주는 편집기 기능입니다. ' +
      '이 앱은 언어마다 후보를 만드는 곳이 다릅니다 — 파이썬은 로컬 Jedi, 자바스크립트는 실행기가 아는 전역, 자바는 표준 클래스 표와 고른 jar 에서 뽑은 멤버 표입니다. 후보를 고르면 필요한 import 줄까지 적어 주기도 합니다.',
  },

  // ── 웹·브라우저 ──
  {
    theme: '웹·브라우저',
    term: 'Blob · Object URL',
    en: 'Blob / object URL',
    match: ['Blob', 'Object URL'],
    body:
      'Blob 은 브라우저 메모리에 든 파일 내용 덩어리이고, Object URL 은 그 덩어리를 잠시 주소처럼 가리키게 만든 blob: 로 시작하는 주소입니다. ' +
      '파일을 내려받게 할 때 이 주소를 링크에 걸어 누릅니다. 다 쓰고 놓아 주지(revoke) 않으면 그 메모리가 계속 남는데, 이 앱은 그 해제를 빠뜨리기 쉬워 내려받기 공용 모듈(MNDownload)로 모았습니다.',
  },
  {
    theme: '웹·브라우저',
    term: '접근성 · aria',
    en: 'accessibility / ARIA',
    match: ['접근성', 'aria'],
    body:
      '눈으로 화면을 보지 못하거나 마우스를 쓰지 못하는 사람도 같은 기능을 쓸 수 있게 하는 것이 접근성이고, aria-* 속성은 화면 읽기 프로그램에 "이 단추가 눌린 상태다" 같은 뜻을 알려 주는 표시입니다. ' +
      '색만으로 구분하지 않기, 키보드로 조작 가능하게 하기, 움직임 줄이기 설정을 따르기가 함께 쓰이는 원칙입니다.',
  },
  {
    theme: '웹·브라우저',
    term: '모달',
    en: 'modal dialog',
    match: ['모달'],
    body:
      '화면 위에 뜬 창이 닫힐 때까지 뒤쪽 화면을 조작하지 못하게 막는 대화상자입니다. ' +
      '이 앱은 모달이 많아서 모든 모달이 Esc 로 닫히는지를 한 번에 전역으로 점검한 적이 있습니다.',
  },

  // ── EXE·네트워크 ──
  {
    theme: 'EXE·네트워크',
    term: '능력 프로브',
    en: 'capability probe',
    match: ['프로브'],
    body:
      '"이 기능을 할 수 있나요?" 만 묻고 yes/no 를 받는 가벼운 요청입니다(/can-run-java, /can-proxy-subway 처럼). ' +
      '이 앱은 "런처가 있다 = 무엇이든 된다" 로 뭉뚱그리지 않고 능력마다 따로 묻습니다 — Go 폴백 런처처럼 어떤 기능은 있고 어떤 기능은 없는 경우가 있기 때문입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: 'Range · 부분 응답',
    en: 'HTTP Range / 206 Partial Content',
    match: ['Range', 'Partial Content'],
    body:
      '파일 전체가 아니라 "몇 바이트부터 몇 바이트까지" 만 달라고 요청하고, 서버가 그 조각만 206 응답으로 돌려주는 방식입니다. ' +
      '영상 재생기가 앞으로 건너뛸 때 이것을 씁니다. 이 앱은 수 GB 영상을 통째로 옮기지 않고 런처가 디스크에서 필요한 조각만 흘려보냅니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: 'Retry-After · 요청 간격',
    en: 'rate limiting',
    match: ['Retry-After', '요청 간격'],
    body:
      '바깥 서버가 "너무 자주 부르니 이만큼 기다렸다 다시 오라" 고 알려 주는 응답 헤더가 Retry-After 입니다. ' +
      '공짜 공개 서비스는 대개 초당·하루 호출 수에 한도가 있어, 이 앱의 런처는 요청 간격을 스스로 지키고 이 헤더가 오면 그만큼 미룹니다. 한도를 넘기면 그날은 기능이 통째로 멈추기 때문입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: 'API 키 · 인증키',
    en: 'API key',
    match: ['인증키', 'API 키', 'REST 키'],
    body:
      '바깥 서비스가 "누가 부르는지" 를 알아보려고 발급하는 문자열입니다. 사용자를 확인하는 로그인과 달리, 가진 사람은 누구든 그 계정으로 호출할 수 있어서 새면 한도를 남이 써 버립니다. ' +
      '이 앱은 키를 화면 코드나 브라우저 저장소에 두지 않고 런처가 들고 대신 붙입니다(지하철 인증키는 "기억하기" 를 고르면 Windows 사용자 계정에 묶어 암호화해 둡니다). 실행마다 새로 만드는 인증 토큰과는 다른 것입니다.',
  },
  {
    theme: 'EXE·네트워크',
    term: 'OSM · OpenStreetMap',
    en: 'OpenStreetMap',
    match: ['OSM', 'OpenStreetMap', 'Overpass'],
    body:
      '누구나 고치고 가져다 쓸 수 있는 공개 지도 데이터입니다. 이 앱의 기본 배경 타일과 장소 검색(Nominatim), 지하철 역 좌표·순서 표(Overpass 조회)가 여기서 옵니다. ' +
      '가져다 쓰는 대신 화면에 출처를 함께 보여야 하고, 짧은 간격으로 대량 호출하지 말아야 한다는 조건이 붙습니다.',
  },

  // ── 코드 개념 ──
  {
    theme: '코드 개념',
    term: '스레드',
    en: 'thread',
    match: ['스레드'],
    body:
      '한 프로그램 안에서 동시에 도는 실행 흐름 하나입니다. 런처는 요청마다, 설치·변환 작업마다 스레드를 씁니다. ' +
      '여러 스레드가 같은 값을 건드리면 순서에 따라 결과가 달라지므로(경쟁 상태) 잠금이 필요하고, 한 스레드가 네트워크를 오래 기다리며 잠금을 쥐고 있으면 다른 요청도 줄줄이 기다리게 됩니다.',
  },
  {
    theme: '코드 개념',
    term: '요청 취소 · 세대 번호',
    en: 'AbortController / request generation',
    match: ['AbortController', '세대 번호', '늦은 응답'],
    body:
      '사용자가 다른 것을 고른 뒤에 예전 요청의 답이 늦게 도착하면, 그 답이 새 화면을 덮어쓰는 사고가 납니다. ' +
      'AbortController 로 예전 요청을 취소하고, 요청마다 세대 번호를 붙여 "지금 번호와 다른 답은 버린다" 를 함께 쓰는 것이 이 앱의 대처입니다 — 취소가 늦어도 번호 검사가 막아 줍니다.',
  },

  // ── 빌드·테스트 ──
  {
    theme: '빌드·테스트',
    term: 'fixture',
    en: 'test fixture',
    match: ['fixture'],
    body:
      '테스트가 매번 같은 조건에서 돌도록 미리 준비해 둔 입력 자료입니다. ' +
      '이 앱은 실시간 지하철·제주 버스처럼 인터넷과 시간에 따라 바뀌는 기능을 한 번 실제로 받아 둔 응답을 fixture 로 저장해, 라이브 호출 없이도 같은 판정을 반복 검증합니다.',
  },

  // ── 파일 형식 ──
  {
    theme: '파일 형식',
    term: 'ffmpeg · MP4',
    en: 'ffmpeg / MP4',
    match: ['ffmpeg', 'MP4'],
    body:
      'ffmpeg 는 거의 모든 영상·소리 형식을 읽고 바꿔 주는 명령줄 도구이고, MP4 는 브라우저가 가장 널리 재생하는 영상 형식입니다. ' +
      '브라우저가 못 여는 mkv·avi 등을 이 앱은 ffmpeg 로 MP4 로 바꾸는데, 속의 영상·소리가 이미 호환되면 다시 인코딩하지 않고 그릇만 옮겨 담아 훨씬 빨리 끝냅니다.',
  },
];

// 각 항목에 고정 id 와, 본문에 걸 링크용 별칭·툴팁용 첫 문장을 붙인다.
//
// 링크 별칭은 match 를 그대로 쓰지 않는다. match 는 "사전이 낡았는지" 검사하는 용도라 '큐'·'락'
// 같은 한 글자도 들어 있는데, 그대로 본문에 걸면 '블락'·'큐레이션' 안쪽까지 잡는다.
// 그래서 두 글자 이상만 남긴다.
const firstSentence = (text) => (/^.*?\.(?=\s)/.exec(text) ?? [text])[0];

export const GLOSSARY_TERMS = TERMS.map((item, index) => ({
  ...item,
  id: `term-${index + 1}`,
  aliases: [...new Set([item.term, ...item.match])].filter((word) => word.length >= 2),
  short: firstSentence(item.body),
}));

const THEMES = ['뼈대', '웹·브라우저', 'EXE·네트워크', '빌드·테스트', '코드 개념', '코드 실행', '파일 형식', '데이터베이스'];

const THEME_GUIDE = {
  뼈대: '이 프로젝트가 왜 이렇게 생겼는지를 설명하는 말들입니다. 여기부터 읽으면 나머지 섹션이 훨씬 수월합니다.',
  '웹·브라우저': '브라우저에서 도는 앱이라면 어디서나 나오는 기본 용어입니다.',
  'EXE·네트워크': 'EXE 와 로컬 서버 섹션을 읽기 전에 알아 두면 좋은 말들입니다.',
  '빌드·테스트': '코드를 만들고 검사하고 내보내는 과정에 쓰는 말입니다.',
  '코드 개념': '어느 섹션에서든 불쑥 나오는 일반적인 프로그래밍 개념입니다.',
  '코드 실행': '학생 코드를 실행·채점하는 섹션(Python·JavaScript·Java)을 읽을 때 필요한 말입니다.',
  '파일 형식': '이 앱이 열고 저장하는 파일들의 속에 관한 말입니다.',
  데이터베이스: 'DB 클라이언트 섹션을 읽을 때 필요한 말입니다.',
};

// 2026-09-03 에 데이터베이스 갈래 11개를 더하면서 여기 THEMES 에는 넣지 않아, 그 용어들은 본문 툴팁만 뜨고
// 사전 화면에는 카드가 한 번도 그려지지 않았다(링크를 눌러도 내려갈 카드가 없음). 2026-09-13 에 발견했다.
// 갈래 목록은 카드가 그려지는 유일한 길이므로, 목록에 없는 갈래가 있으면 생성을 멈춘다.
const orphanThemes = [...new Set(TERMS.map((item) => item.theme))].filter((theme) => !THEMES.includes(theme));
if (orphanThemes.length) throw new Error(`용어 사전 THEMES 에 없는 갈래: ${orphanThemes.join(', ')}`);

export default ({ helpers }) => {
  const { sec } = helpers;

  const byTheme = (theme) => GLOSSARY_TERMS.filter((item) => item.theme === theme);

  return [
    sec({
      id: 'glossary',
      category: '개요',
      group: '용어 사전',
      title: '용어 사전',
      subtitle: `초급자가 막히는 말 ${TERMS.length}개 — 이 리뷰에 실제로 나오는 것만`,
      summary:
        `이 리뷰에 나오는 말 중 처음 보면 문장 전체를 놓치게 되는 ${TERMS.length}개를 모았습니다. ` +
        '고를 때 짐작하지 않았습니다 — 리뷰 산문의 어휘 빈도를 세어, 자주 나오면서도 설명 없이는 넘어가기 어려운 말만 담았습니다. ' +
        '마지막으로 센 2026-09-13(산문 26만 자) 기준으로 가장 많이 나오는 말은 "전역"(192회)·"계층"(189회)이고 그다음이 "의존"(104회), "빌드"(95회), "채점"(70회)입니다. ' +
        '즉 이 다섯 개만 알아도 리뷰 문장의 체감 난이도가 크게 내려갑니다. 찾을 때는 Ctrl+K 로 용어를 바로 검색하세요.',
      usage: THEMES.map((theme) => ({
        title: `${theme} (${byTheme(theme).length}개)`,
        body: `${THEME_GUIDE[theme]} — ${byTheme(theme)
          .map((item) => item.term)
          .join(', ')}`,
      })),
      features: THEMES.flatMap((theme) =>
        byTheme(theme).map((item) => ({
          // id 는 본문의 용어 링크가 이 카드로 정확히 내려오기 위한 앵커다.
          id: item.id,
          // 용어 자체에 가운뎃점이 든 것이 많아(‘큐 · 락’) 영문 병기는 괄호로 묶는다.
          title: item.en ? `${item.term} (${item.en})` : item.term,
          body: item.body,
        })),
      ),
      files: [],
      notes: [
        {
          type: 'info',
          label: 'Info',
          body:
            '이 사전은 일반적인 용어집이 아니라 이 프로젝트에 맞춘 것입니다. 같은 말이라도 다른 프로젝트에서는 조금 다르게 쓰일 수 있으니, ' +
            '설명의 뒷부분("이 앱에서는 …")을 함께 읽으세요.',
        },
        {
          type: 'info',
          label: 'Info',
          body:
            '사전에만 있고 리뷰 본문에는 더 이상 나오지 않는 말이 생기면 생성 스크립트가 경고합니다. ' +
            '리뷰 문장이 바뀌어 안 쓰게 된 말이 사전에 조용히 남아 있는 것을 막기 위해서입니다.',
        },
        {
          type: 'good',
          label: 'Good',
          body:
            '모르는 말이 나왔을 때 검색(Ctrl+K)하면 이 사전 항목과 그 말이 실제로 쓰인 섹션이 함께 걸립니다. ' +
            '뜻을 확인한 다음 바로 사용 예로 넘어가는 순서로 읽으면 좋습니다.',
        },
      ],
    }),
  ];
};
