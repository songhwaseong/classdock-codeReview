# 만능파일교실 코드리뷰 맵

`D:\my\myOffice` 소스를 읽어 만든 정적 코드리뷰 사이트입니다.
**`index.html` 을 브라우저로 열면 바로 뜹니다** — 서버도 빌드도 인터넷도 필요 없습니다.

## 무엇이 들어 있나

| | 수 |
|---|---|
| 섹션 | 106개 (10개 대분류) |
| 실린 소스 | 195개 파일 · 108,232줄 |
| 리뷰 포인트 | 274개 (Good 128 / Risk 98 / Info 48) |
| 용어 사전 | 68개 (6개 갈래) |
| 코드 낱말 사전 | 77개 (JavaScript · C# · Python) |
| 코드 기호·기본 기능 사전 | 80개 (기호 25 + 기본 기능 55) |
| 코드 메서드·실행 패턴 사전 | 96개 (메서드 81 + 패턴 15) |
| 프로젝트 API 사전 | 150개 (공유·반복 호출 함수) |
| 줄 앵커 주석 | 328개 (파일 개요 258 + 지목 70) |
| 흐름 추적 | 14개 (총 91단계) |
| 계약 | 32개 (전역 API 10 + EXE 엔드포인트 22) |
| 자동 생성 다이어그램 | 5개 (SVG) |

기준 소스: `myOffice` 2026-08-12 시점.

대분류: 개요 · 1.bootstrap · 2.documents · 3.python·notebooks · 4.javascript ·
5.document-editors · 6.learning-tools · EXE·로컬 서버 · 빌드·도구 · 테스트

## 다시 만들기

소스를 고친 뒤 리뷰 페이지에 반영하려면:

```bash
node D:/my/code-review/generate-review-data.mjs
```

소스 루트는 `MN_ROOT` → 부모 폴더 → 형제 폴더 `myOffice` 순으로 자동으로 찾습니다.
다른 위치라면 직접 지정하세요:

```bash
MN_ROOT=D:/my/myOffice node D:/my/code-review/generate-review-data.mjs
```

PowerShell 에서는:

```bash
$env:MN_ROOT="D:\my\myOffice"; node D:\my\code-review\generate-review-data.mjs
```

## 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 껍데기. 사이드바 + 7개 패널 + 코드 모달 |
| `styles.css` | 라이트/다크 테마, 반응형. 외부 폰트 없음(시스템 한글 글꼴 스택) |
| `app.js` | 렌더러 — 섹션·검색·구문강조·주석·다이어그램 확대/이동·모달 |
| `generate-review-data.mjs` | 생성 스크립트(진입점) |
| `lib/diagram.mjs` | `scripts.manifest.json` 에서 SVG 다이어그램을 만드는 코드 |
| `lib/section.mjs` | 섹션 정의 도우미(로딩 순서·의존·공개 API 자동 주입) |
| `lib/project-functions.mjs` | 프로젝트 함수 선언·호출 분석과 상위 150개 선정 |
| `sections/*.mjs` | **사람이 쓰는 리뷰 문장**. 여기를 고칩니다 |
| `review-comments.js` | 줄 앵커 주석 |
| `flows.js` | 흐름 추적 |
| `contracts.js` | 전역 API·EXE 엔드포인트 계약 |
| `review-data.generated.js` | **자동 생성물(약 7.0MiB). 직접 고치지 마세요** |

## 설계 메모

- **다이어그램은 손으로 그리지 않았습니다.** 계층도·의존 그래프·공개 API 경계도는
  `scripts.manifest.json` 을 읽어 생성 시점에 SVG 로 만듭니다. manifest 가 바뀌면
  다이어그램도 따라 바뀌므로 그림이 코드와 어긋나지 않습니다.
- **CDN 의존 0.** 원본 앱이 오프라인 우선이라 리뷰 산출물도 같은 원칙을 지켰습니다.
  Mermaid 같은 런타임 렌더러도, 웹폰트도 쓰지 않습니다.
- **큰 파일은 구간만.** `desktop/launcher.cs`(7,257줄)는 기능별 구간으로 잘라 실었고,
  줄 번호는 원본 기준 그대로입니다. 화면에도 `L1800–L1900 / 전체 7,257줄` 처럼 표시됩니다.
- **정합성은 생성 시 검사.** 섹션 id 중복, 빈 요약, 읽지 못한 파일, 섹션에 실리지 않은
  `src/js` 파일과 `docs` 문서, 상한을 넘겨 꼬리가 잘린 파일을 생성 스크립트가 경고합니다.
  소스에 새 설계 문서가 생기거나 파일이 자라 리뷰에서 일부가 빠지면 다음 생성 때 드러납니다.
- **테스트는 전수가 아니라 표본.** 단위 78/102 · E2E 13/37 을 싣습니다. 기능별 대표를 고른
  것이라 미참조 테스트는 경고 대상이 아니고, 대신 생성 스크립트가 매번 이 비율을 찍습니다.
- **용어 사전도 검사 대상.** `sections/05-glossary.mjs` 의 68개 항목은 짐작이 아니라 리뷰 산문
  21만 자의 어휘 빈도로 골랐습니다(전역 850회, 계층 169회, 폴백 68회 …). 사전에는 있는데 리뷰
  본문에는 더 이상 나오지 않는 말이 생기면 생성 스크립트가 경고하므로, 문장을 고치는 동안 사전이
  조용히 낡지 않습니다.
- **용어 링크는 첫 등장에만.** 본문에 나오는 사전 용어에 툴팁과 링크를 자동으로 걸되, 한 화면에서
  용어당 첫 번째 등장 하나만 겁니다. `전역` 은 리뷰에 850번 나오므로 전부 걸면 밑줄 범벅이 됩니다.
- **코드 낱말 사전은 실제 사용분만.** 코드 뷰어에서 쓰는 JavaScript·C#·Python 문법 낱말 77개를
  설명합니다. 함수명·변수명·라이브러리 API와 연산자는 1차 범위에서 제외했습니다.
- **2차는 기호와 표준 기본 기능만.** 실제 코드에 나오는 연산자·기호 25개와 브라우저·.NET·Python
  표준 기본 기능 55개를 설명합니다. 프로젝트 전용 이름과 외부 패키지는 계속 제외합니다.
- **3차는 표준 메서드와 실행 패턴.** 객체 뒤에 점을 찍어 쓰는 표준 메서드 81개는 코드와 연결하고,
  콜백·이벤트 위임·디바운스처럼 여러 줄에 걸친 패턴 15개는 검색과 사전 카드로 설명합니다.
- **4차는 프로젝트 API만.** 실제 프로젝트 선언을 기준으로 사용 파일 수와 호출 횟수를 계산하고,
  기능 영역별 몫을 둬 상위 150개만 남깁니다. 표준·외부 API와 여러 곳의 독립 함수가 우연히 같은
  이름을 쓰는 경우는 제외하며, 코드에서는 괄호가 뒤따르는 호출 형태에만 연결합니다.
  섹션당 12–24개, 대략 한 문단에 하나꼴입니다. 코드 조각 안과 사전 자신은 걸지 않습니다.

## 조작

| | |
|---|---|
| `Ctrl+K` | 검색창으로 |
| 점선 밑줄 용어에 마우스 올리기 | 뜻 미리보기(툴팁). 키보드는 `Tab` 으로 옮겨도 같이 뜨고 `Esc` 로 닫습니다 |
| 점선 밑줄 용어 클릭 | 용어 사전의 해당 항목으로 이동 |
| 코드의 점선 밑줄 낱말에 마우스 올리기 | 언어와 짧은 뜻 미리보기 |
| 코드의 점선 밑줄 낱말 클릭 | 코드 낱말 사전의 해당 항목으로 이동 |
| 코드의 기본 기능·연산자 클릭 | 코드 기호·기본 기능 사전의 해당 항목으로 이동 |
| 코드의 점선 밑줄 메서드 클릭 | 코드 메서드·실행 패턴 사전의 해당 항목으로 이동 |
| 코드의 프로젝트 함수 클릭 | 프로젝트 API 사전의 해당 항목으로 이동 |
| 사이드바 경계 드래그 | 너비 조절 (더블클릭 = 기본값) |
| 다이어그램 휠 / 드래그 | 확대 / 이동 |
| 흐름·계약의 파일 위치 클릭 | 해당 코드 줄로 이동(모달) |
| 우상단 ☾/☀ | 테마 전환 |
